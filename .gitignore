# Secrets / env — never commit
.env
.env.*
*.local
supabase/.env
supabase/.temp/

# Supabase CLI local state
supabase/.branches/
.supabase/

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Node / Next.js
node_modules/
dist/
build/
.next/
out/
next-env.d.ts
tsconfig.tsbuildinfo
.vercel

# Sub-agent worktrees (created by Agent tool with isolation: worktree)
.claude/
.claire/

# Bronwyn's contract templates — provenance only, model as data
_contract_templates/

# Scratch: files moved out of the tree pending deletion by Simon
_to_delete/

# Raw email drops (source material, not code)
*.eml
